= simple_form_for(@blog) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :description
    = f.association :event
    = f.association :church
    = f.association :user

  .form-actions
    = f.button :submit
